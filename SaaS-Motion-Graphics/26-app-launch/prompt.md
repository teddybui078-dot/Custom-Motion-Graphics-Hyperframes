# Prompt — 26-app-launch

The original prompt used to generate this motion graphic:

Duration: 10 seconds

A phone frame centered on white canvas — minimal chrome, just the outline and screen. The screen is black — the app is loading. A centered logo mark fades in on the black screen, scales up slowly from 0.9 to 1.0 over 1 second, then the entire screen transitions — a fast fade from black to the app's home screen. The home screen is fully populated: a greeting at the top, a card stack below it, a bottom navigation bar. The elements inside the home screen build in with a staggered fade-up, top to bottom, 8 frames between each section. The bottom nav icons pop in last, simultaneously. The app is alive.
