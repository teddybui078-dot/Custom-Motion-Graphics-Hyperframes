# Prompt — 03-keynote-metrics

The original prompt used to generate this motion graphic:

hyperframes now create this: On a light gray canvas (#F5F5F7), three large metric cards fade up in sequence — "10x faster", "99.9% uptime", "2M+ users". Each number counts up from zero using an eased counter animation, reaching its final value at slightly different times (staggered by 12 frames). Behind each card, a thin circular progress ring draws itself clockwise in a matching accent color (blue, teal, coral). Once all three land, a soft horizontal rule slides in underneath and a one-line descriptor fades in below each metric. No motion after that — total stillness. Duration: 7 seconds. Feeling: a keynote slide that earns silence.
