# Prompt — 09-command-bar-portal

The original prompt used to generate this motion graphic:

Command Bar Portal

"A SaaS command bar (Cmd+K) opens as a 3D portal — the background recedes in depth, the search bar extrudes forward toward the camera with a glass material. Results fly in from the depth field, each a 3D card on a z-axis track. Selected result rockets forward and expands to fill screen."
