# Prompt — 03-dynamic-island

The original prompt used to generate this motion graphic:

iPhone Dynamic Island Interaction

"Build a Dynamic Island animation sequence: idle pill state → expands to show a music player with album art and waveform → shrinks back → expands again for an incoming call with avatar glow → user taps to dismiss with a spring compression. White ambient glow on island edges."
