# Prompt — 02-app-store-expand

The original prompt used to generate this motion graphic:

Create a 16:9 4K UI animation of an App Store card expanding from a thumbnail into a full-screen preview. The card should lift off with a soft shadow bloom, blur the background, and reveal a scrollable screenshot reel. Use spring physics for the expansion and a frosted glass close button that fades in. White corner glows on the card edges.

store this in the directory cool-vids (create it)
