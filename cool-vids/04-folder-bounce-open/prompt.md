# Prompt — 04-folder-bounce-open

The original prompt used to generate this motion graphic:

Folder Bounce-Open

"Recreate the macOS folder open animation as a UI sequence: a folder icon sits in a dock, mouse clicks it, the icon does a 3-bounce spring jump, folder explodes open into a Finder window with spring overshoot on the window corners. Add a radial light bloom on click impact."
